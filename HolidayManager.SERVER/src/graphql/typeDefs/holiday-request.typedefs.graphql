type HolidayRequest {
    _id: String!
    requestReference: String!
    dates: [Date!]!
}

type Date {
    date: String!
}

type Query {
    holidayRequest(id: String!): HolidayRequest
} 

type Mutation {
    createHolidayRequest(name: String!): HolidayRequest
}