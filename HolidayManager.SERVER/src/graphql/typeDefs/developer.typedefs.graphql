type Query {
  developer(_id: String!, unitId: String): Developer
}

type Mutation {
  createDeveloper(name: String!, unitId: String!, referenceId: String!): Boolean!
}

type Developer {
    _id: String!
    name: String!
    role: String!
    createdOn: String!
    unit: String
    referenceId: String
    holidayRequests: [HolidayRequest]
    projects: [Project]
    projectManagers: [ProjectManager]   
}